# 物理可视化闸门规则（强制执行）

本文件优先级高于页面美术与动画效果。任何题目在生成或修改最终 HTML 前，必须依次通过以下闸门；任一闸门 FAIL，禁止发布“完成版”链接。

## Gate 0｜原题输入完整性

必须先确认：题干、原图、已知条件、研究对象、问题/选项是否完整。若原图承担空间关系、几何比较或参照系作用，必须把原图列为一级输入，不能只依赖文字题干。

## Gate 1｜物理规格（此阶段禁止写 HTML）

逐项写清：
1. 世界坐标系与正方向；
2. 场景对象与研究对象；
3. 初始条件；
4. 几何/接触/绳长/轨道等约束；
5. 全部真实受力及作用点；
6. 运动阶段与阶段切换条件；
7. 独立参数；
8. 派生参数；
9. 运动方程/约束方程；
10. 关键事件、终止条件；
11. 守恒量或应满足的不变量；
12. 可能错误与合法参数边界。

不得用屏幕坐标、预设终点、美术曲线或动画时长反推物理运动。

## Gate 2｜原图几何规格

若原图承担物理信息，必须建立“原图对象 → 世界对象 → HTML对象”逐项映射，并核对：
- 观察视角/透视拓扑；
- 坐标轴与正方向；
- 平面、斜面、地面、墙面、轨道；
- 固定点、关键点、落点；
- 实线/虚线表示的可见与隐藏关系；
- 同高、共线、平行、垂直、接触等关系；
- 原题用于比较的视觉基准。

要求“与原题一致”时，含义是拓扑、参照系、关键点、平面关系、方向和比较基准逐项对应，不是仅风格相似。未经确认不得换视角、降维或重新构图。

## Gate 3｜物理模型与视觉投影分离

必须采用：

`独立参数 → 物理方程 → 世界坐标/世界矢量 → 统一投影函数 → SVG/Canvas`

禁止：
- 用 Bezier/手绘轨迹代替真实运动方程；
- 先指定屏幕终点再让物体“走过去”；
- 为适应画面修改真实运动时间、速度或受力；
- 单独修改速度箭头方向以配合美术；
- 让同一物理对象的轨迹、速度、受力来自不同模型。

## Gate 4｜数值/物理自动自检

每题至少检查：
- t=0 初始条件；
- 一个中间时刻；
- 每个关键事件；
- 终止状态；
- 参数最小值；
- 参数最大值；
- 约束残差；
- 受力方向；
- 速度是否为位置对时间导数；
- 加速度是否与合力/m一致；
- 应守恒的量是否在容差内守恒。

任一核心检查 FAIL，不得进入最终 HTML。

## Gate 5｜视觉/交互验收

物理通过后才检查：
- 原图结构是否仍可一眼对应；
- 作用点、箭头、标签是否正确且不悬空；
- 固定参照是否被动态缩放破坏；
- 开始/暂停、重置、0.5×/1×/2×、进度条、参数调节是否正常；
- 参数变化是否同步更新全部物理量；
- 不适合动态的内容是否被强行动画化；
- 关键对象在参数极值下是否仍可见。

## Gate 6｜发布条件

只有 Gate 0–5 全部 PASS 才能：
1. 生成/更新最终 HTML；
2. 标记为“可核查版/完成版”；
3. 向用户发送正式链接。

若未通过，只能发送规格核查或问题清单，不得把未验收页面称为最终版。

## Codex / Hermes / Claude 执行短指令

> 先读 `PHYSICS_VISUAL_STANDARD_V1.md`、`PHYSICS_VISUAL_EXPRESSION_RULES.md`、`PHYSICS_VISUAL_GATE_RULES.md`。先完成 Gate 0–2 的文字规格，禁止先写 HTML。随后只允许用“物理方程→世界坐标/矢量→统一投影”生成画面。完成 Gate 4 数值自检和 Gate 5 交互验收；任一核心项 FAIL，停止发布并报告问题。