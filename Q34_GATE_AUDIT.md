# 第34题 Gate 核查记录

状态：**Gate 0 未完成；禁止继续生成“最终版”HTML。**

## Gate 0｜题意与对象
当前仓库仅能确认旧页面假设了以下模型：
- 电梯质量 M；
- 电梯内物体质量 m；
- 电梯由静止开始向上匀加速；
- 上升高度 H；
- 末速度 v；
- 钢索拉力 T；
- 物体所受支持力 N；
- 旧页面使用整体关系 `T-(M+m)g=(M+m)a`，以及 `N-mg=ma`。

但仓库没有保存第34题完整原题图片/题干/选项，无法确认：
- 原题究竟问钢索做功、支持力做功、重力做功、动能、机械能中的哪些量；
- H、v、M、m 是否为题设直接给定参数；
- 电梯是否确实从静止开始匀加速；
- 原题图中是否有必须保留的几何/受力结构；
- 选项需要核查的对象究竟是“物体 m”“电梯 M”还是“整体 M+m”。

因此不能仅依据旧页面继续推断原题并改成最终版。

## Gate 1｜当前旧模型的物理自检
在“由静止向上匀加速、上升 H、末速度 v”这一假设下：

`a=v^2/(2H)`

对物体 m：

`N=m(g+a)`

支持力对物体做功：

`W_N=N H = m(g+a)H`

重力对物体做功：

`W_g=-mgH`

物体动能增量：

`ΔK_m=1/2 m v^2 = maH`

满足：

`W_N+W_g=ΔK_m`

即：

`W_N = ΔK_m + mgH`

对整体 M+m：

`T=(M+m)(g+a)`

钢索做功：

`W_T=T H`

整体满足：

`W_T-(M+m)gH=1/2(M+m)v^2`

即：

`W_T=ΔK_total+ΔU_g,total`

以上关系在该假设模型内自洽，但**不能据此证明它就是原题模型**。

## Gate 2｜原题几何规格
未执行。需要完整原题图片后，逐项确认：电梯、物体、钢索、位移方向、题目给出的图示、标注与比较基准。

## Gate 3｜世界模型 → 投影
待 Gate 0/2 通过后执行。页面中运动位置、速度、受力箭头、功与能量数值必须来自同一套方程。

## Gate 4｜自动自检要求
最终版至少检查：
- `v^2=2aH`（若原题确为由静止匀加速）；
- `N-mg=ma`；
- `T-(M+m)g=(M+m)a`；
- `W_N+W_g=ΔK_m`；
- `W_T-(M+m)gH=ΔK_total`；
- 参数极值下箭头、运动、功和能量均保持同源；
- 若原题不是上述模型，全部按原题重建，不保留旧假设。

## 当前结论
旧 `q34-energy-flow-v4.html` 只能视为“一个自洽的电梯匀加速示例”，不能确认是第34题最终正确可视化。请先提供第34题完整原题图片，然后继续 Gate 0 → Gate 2 → 最终 HTML。