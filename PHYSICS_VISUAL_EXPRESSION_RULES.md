# 物理可视化表达方式补充标准

本文件与 `PHYSICS_VISUAL_STANDARD_V1.md` 同时执行。目标不是让网页替代老师讲解，而是把适合“看见”的物理过程做得真实、清楚、可调、可重复。

## 1. 项目定位

**本项目首先是动态演示工具，不是完整讲解器。**

网页主要负责：

- 把运动、形变、碰撞、轨迹、速度方向、波传播、几何变化等过程真实地“动起来”；
- 通过参数调整，让老师和学生观察不同条件下过程如何变化；
- 保留必要的受力、速度、角度、位置、关键数值标注，便于老师现场讲解；
- 对不适合连续动态展示的物理量，不强行做动画。

老师负责进一步解释公式、推导、因果关系、解题方法和结论。

## 2. 动态展示优先级

优先动态展示肉眼能直接感知、且动态能明显提升理解的内容：

- 位置变化
- 轨迹
- 速度方向和明显的速度大小变化
- 碰撞过程
- 弹簧伸缩
- 绳、杆、滑轮等几何变化
- 转动
- 波传播与质点振动
- 接触、脱离、反向、骤停
- 物体之间相对位置变化
- 题目明确要求比较的几何量变化

若某个量通过连续动画无法被可靠看懂，则不强行做动态。

## 3. 不适合强行动画化的内容

以下物理量默认只做必要标注、数值、静态辅助或可选图像，不要求通过主体运动直接表现：

- 加速度
- 功
- 动能、势能、机械能
- 动量、冲量
- 力矩
- 守恒量
- 极值条件
- 纯公式关系
- 仅靠运动外观无法可靠判断的量

这些内容只有在“图像或辅助表达确实比老师口头讲解更直观”时才加入。

## 4. 参数调整是核心功能

凡题目中存在可合理变化的独立参数，应优先提供可调控件，并保证调整后仍满足真实物理规律。

常见参数包括：

- 质量 m、M
- 初速度 v₀
- 外力 F
- 弹簧劲度系数 k
- 摩擦因数 μ
- 斜面角 θ
- 波长、频率、振幅
- 绳长、杆长、几何位置
- 电压、电场强度等题目允许变化的物理量

参数区原则：

1. 放在主画面右侧或不遮挡主动画的位置；
2. 调整立即更新动画和标注；
3. 重置必须恢复初始参数；
4. 参数范围必须限制在物理上合法、视觉上可用的区间；
5. 独立参数可调，派生参数自动计算，不把派生量伪装成独立参数。

## 5. 交互必须保留

除非题目本身不需要播放，否则优先保留：

- 开始/暂停合并按钮
- 重置按钮
- 0.5× / 1× / 2× 播放速度
- 进度拖动（适合时）
- 题目允许的对象拖动
- 显示/隐藏受力或辅助信息（适合时）
- 参数滑块

不得为了增加“解释层”而删除原有的拖动、按钮、标注或参数设置。

## 6. 标注规则

动态页面必须保留必要的物理标注，帮助老师指着页面讲解：

- 物体名称
- 受力名称与方向
- 速度箭头
- 关键角度
- 关键长度/位置
- 必要的实时数值

标注必须跟随对象变化，避免遮挡、重叠、悬空或与真实作用点脱离。

## 7. 受力与物理正确性

所有运动和参数变化必须服从同一套物理模型。

- 重力始终竖直向下；
- 支持力垂直接触面；
- 摩擦力方向与相对运动/相对运动趋势相反；
- 绳张力沿绳方向；
- 弹簧力与形变量一致；
- 速度、位置、受力、图像和数值必须同源；
- 不得为了视觉效果修改物理规律。

## 8. 比较型题目的固定参照

当题目要求比较角度、长度、相对位置或几何形状时：

- 参照系必须锁定；
- 视觉比例必须锁定；
- 不使用动态镜头缩放；
- 不使用自动 viewBox 改变比较基准；
- 不使用相机跟随破坏视觉比较。

## 9. 辅助内容的使用原则

辅助受力图、v-t/a-t图、状态对照、能量图等只能在确有价值时加入。

优先做成：

- 可展开区域
- 可选显示
- 不长期占据主动画空间

如果辅助内容只是增加页面复杂度、不能明显帮助观察，则不加入。

## 10. 制作前强制判断

修改 HTML 前先回答：

1. 这道题最值得动态展示的是什么？
2. 哪些量可以通过动画直接看见？
3. 哪些量不适合动态，应该交给老师解释或只做简单标注？
4. 哪些参数值得让用户调整？
5. 参数变化后，运动和受力是否仍满足物理规律？
6. 是否保留开始/暂停、重置、拖动、速度控制和必要标注？

## 11. 验收标准

每道题至少检查：

1. 初始状态；
2. 运动中间状态；
3. 关键事件；
4. 终止状态；
5. 参数最小值；
6. 参数最大值；
7. 拖动/按钮/重置是否正常；
8. 受力、速度、位置、图像和数值是否同源；
9. 主动画是否真正“动起来”；
10. 是否存在不适合动态却被强行动态化的内容；
11. 是否因为增加辅助信息而破坏原有交互、标注或参数设置。

## 12. 给 Codex / Hermes / Claude 的执行指令

> 先读取 `PHYSICS_VISUAL_STANDARD_V1.md` 和 `PHYSICS_VISUAL_EXPRESSION_RULES.md`。本项目优先做真实、清晰、可交互的动态物理演示，不要求网页替代老师讲解。先识别最适合动态展示的运动、轨迹、速度、碰撞、伸缩、转动、波传播和几何变化，再确定可调独立参数。保留开始/暂停、重置、速度控制、拖动、必要标注和参数区。加速度、功、能量、动量、力矩、守恒和极值等若不适合直接动态展示，不强行动画化，只在确有帮助时做简单可选辅助。所有参数变化必须继续满足真实物理规律。完成后按初始、中间、关键事件、终止、参数极值和交互功能逐项自检。