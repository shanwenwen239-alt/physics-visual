# 物理可视化表达方式补充标准

本文件与 `PHYSICS_VISUAL_STANDARD_V1.md` 同时执行，用于决定“什么应该动画化，什么不应该强行动画化”。若两者冲突，以物理正确性和教学清晰度优先。

## 1. 核心原则

**主体动画只承担肉眼可以直接感知的物理变化。**

适合直接动画化的量包括：

- 位置
- 轨迹
- 速度方向与明显的速度大小变化
- 碰撞
- 弹簧伸缩
- 绳、杆、滑轮等几何变化
- 转动
- 波形传播
- 接触、脱离、反向、骤停等关键事件

不应强行依赖主体运动来表达的量包括：

- 合力
- 加速度
- 功
- 动能、势能、机械能
- 动量、冲量
- 力矩
- 守恒量
- 极值条件
- 难以凭肉眼从主体运动中可靠判断的趋势量

这些量优先进入“解释层”。

## 2. 强制判断准则

如果学生关闭数值后，仍然无法仅凭动画可靠看出某个物理量的变化趋势，就不要把它当作主要动态表达对象。

连续动画只有在显著提升理解时才使用；不为了“动态”而动态。

## 3. 五种表达方式

每道题制作前，必须逐项判断核心物理量属于以下哪种表达方式：

### A. 主动画
用于肉眼可直接感知的连续变化：位置、轨迹、速度方向、碰撞、伸缩、转动、波传播、几何约束变化等。

### B. 辅助图像
用于连续变化但不容易直接从主体运动判断的量：a-t、v-t、x-t、F-t、a-x、能量随时间/位置变化等。

### C. 状态比较
用于关键状态、前后状态、极值状态、相等条件、临界条件。优先采用冻结关键时刻、并排状态、关键事件卡片。

### D. 系统量视图
用于整体法和守恒关系：功、能量、动量、冲量、力矩、质心、系统机械能等。可采用能量流、能量仓、动量箭头、系统边界、守恒账本等。

### E. 不需要动态
若连续动画不能增加理解，或题目核心是静力平衡、几何比较、纯状态判断，则采用静态交互、参数拖动、状态切换或辅助图，不强制播放动画。

## 4. 加速度专项规则

加速度默认不通过主体运动本身表达。

优先顺序：

1. 合力箭头或合力关系；
2. `F合 = ma` 对应关系；
3. a-t / a-x 等辅助图；
4. 关键时刻：a=0、a相等、a最大/最小；
5. 必要时才显示加速度箭头。

主体动画仍负责展示与加速度结果相关的真实运动，但不得要求学生仅凭位置变化去反推出加速度。

## 5. 功和能量专项规则

功、动能、势能和机械能优先使用系统量视图，不靠主体动画直接表达。

- “能量从哪里来、到哪里去”优先用能量流；
- “各类能量当前占多少”优先用能量仓、能量珠或比例视图；
- “前后状态比较”优先用状态对照；
- “守恒”应通过总量固定、系统边界或账本关系直接体现。

## 6. 动量和冲量专项规则

- 碰撞主体动画负责真实碰撞和速度方向；
- 动量优先用长度与 p=mv 对应的动量箭头；
- 系统总动量用系统量视图或碰撞前后对照；
- 冲量优先用 F-t 面积或 Δp 对照，不靠运动外观猜测。

## 7. 力矩与极值专项规则

- 力矩优先显示力、力臂、转轴和方向，再进入解释层显示 τ；
- 极值条件必须单独突出关键事件，不依赖用户“看出来”；
- 当某个物理量达到 0、相等、最大、最小或临界值时，可自动暂停、高亮或显示关键状态卡片。

## 8. 制作前新增强制输出

在原有题目规格基础上，增加：

1. 每个核心物理量对应哪一种表达方式：主动画 / 辅助图像 / 状态比较 / 系统量视图 / 不需要动态；
2. 哪些量即使有数值，也不能作为主体动画的主要表达对象；
3. 哪些关键时刻需要冻结、高亮或自动提示；
4. 如果关闭实时数值，学生仍能从哪些视觉元素理解题目核心；
5. 若无法回答第4项，则必须调整表达方式后再写 HTML。

## 9. 验收新增项目

除原五类状态验收外，增加“表达方式验收”：

- 主动画是否只承担可直接感知的物理变化；
- 难感知量是否有合适的解释层；
- 是否存在为了动态而动态的内容；
- 关闭数值后，核心教学信息是否仍能通过主动画 + 解释层理解；
- 关键状态和极值是否被明确标出；
- 系统量与局部量是否混淆；
- 辅助图像是否与主物理模型同源。

## 10. 给 Codex / Hermes / Claude 的执行指令

> 先读取 `PHYSICS_VISUAL_STANDARD_V1.md` 和 `PHYSICS_VISUAL_EXPRESSION_RULES.md`。先做物理规格，再给每个核心物理量标注表达方式：主动画 / 辅助图像 / 状态比较 / 系统量视图 / 不需要动态。主体动画只承担肉眼可直接感知的变化；合力、加速度、功、能量、动量、力矩、守恒和极值优先进入解释层。若关闭数值后无法可靠看出某物理量趋势，不得把它作为主体动画主要表达对象。完成后再修改 HTML，并按初始、中间、关键事件、终止、极值参数和表达方式六类标准自检。
