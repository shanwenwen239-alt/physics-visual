# 物理可视化 HTML 统一标准 V1

> 用途：作为 ChatGPT / Claude / Codex / Hermes / WorkBuddy 批量生成和验收高中物理可视化网页的唯一上位标准。
> 
> 原则：先保证物理世界正确，再保证交互有效，最后统一视觉。不得用“看起来像”代替“物理上正确”。

## 1. 产品目标

每道题不是静态插图，而是一个可操作的“小型物理世界”。页面必须帮助学生看见：

**题意 → 初始条件 → 关键事件 → 受力/约束 → 物理量变化 → 图像/数据 → 结论**。

默认输出为可直接打开的 HTML 交互页面，不生成图片，除非明确要求。

## 2. 页面统一结构

桌面端默认两栏：

- 左侧：主物理场景，必要时紧接 v-t / x-t / F-t 等图像，不用播放控制把主动画和图像隔开。
- 右侧：播放控制 → 参数调整 → 实时物理量 → 必要的物理核查/结论。

移动端可以自动改成上下布局。

### 播放控制

- “开始 / 暂停 / 继续 / 重新开始”使用同一个主按钮。
- 保留“重置全部”。
- 默认提供 0.5× / 1× / 2×。
- 可以提供进度条；拖动进度条或参数时自动暂停。
- 重置全部必须恢复初始位置、初始状态和默认参数。

## 3. 物理世界规则（最高优先级）

### 3.1 世界坐标与局部坐标

- 重力 G 永远在世界坐标中竖直向下。
- 斜面、物体、相机或透视旋转，不得带着重力一起旋转。
- 透视只能改变“怎么看”，不能改变真实的高低、前后、运动方向和受力关系。

### 3.2 接触与连接

- 物体必须在接触面上，不能嵌入斜面、木板或穿模。
- 弹簧两端必须始终连接真实固定点/物体表面，运动时不可断开。
- 绳必须连接固定点、滑轮或物体，绳长不变时必须严格满足几何约束。
- 杆、轨道、墙面、铰链等必须保持真实几何关系。

### 3.3 力的方向

- G：始终竖直向下。
- N：始终垂直接触面并指向物体。
- f：沿接触面，方向反对相对运动或相对运动趋势。
- Ft / T：始终沿绳方向。
- 弹簧力：沿弹簧轴线，方向符合压缩/伸长恢复趋势。
- 外力 F：从真实施力点或重心附近出发，方向严格符合题意。
- 电场力、洛伦兹力等必须由题设电荷、电场、速度、磁场实时计算，不得手绘固定方向。

### 3.4 力的大小

- 力箭头长度应随力的大小同步变化；同屏可比较时保持统一尺度。
- 若力为 0：箭头隐藏或缩为 0，并明确数值 0；不得留下误导性残箭头。
- 同一题内同类力必须使用相同缩放比例。

### 3.5 题设约束优先

若题设规定：匀速、平衡、绳长不变、光滑、完全弹性碰撞、无阻力等，则参数调整不得破坏这些条件。

例如：题设要求“匀速下滑”，若调整 θ、m、μ，应自动反算满足平衡的 F，而不是让 F 独立变化后仍显示“匀速”。

## 4. 力矢量视觉规范

### 4.1 箭头

- 采用高中教材 / PhET / GeoGebra 风格。
- 线条细而清晰，禁止粗大流程图箭头和夸张三角箭头。
- 箭头尖短小、利落，圆润抗锯齿。
- 箭头从真实作用点或物体重心附近出发，不得悬空。

### 4.2 标签

- F、G、N、f、Ft 等标签放在箭头附近，但不压在线上、不遮物体。
- 动画中标签位置必须同步移动，避免重叠。

### 4.3 建议统一颜色

颜色只做语义辅助，不代替文字：

- 重力 G：红色实线
- 支持力 N：绿色实线
- 摩擦力 f：橙色实线
- 外力 / 张力 / 弹簧力：蓝色或金色实线（同题内保持一致）
- 速度 v：蓝色虚线
- 加速度 a：紫色虚线或单独数据表示

若题目需要更合适的配色，可调整，但必须全项目一致。

## 5. 运动与动画

- 所有位置、方向、大小变化必须连续平滑，不得跳变、闪烁、瞬移。
- 运动必须由物理方程或分段物理事件驱动，不得只做装饰性 tween 动画。
- 关键事件要真实发生，例如：骤停、脱离、碰撞、反向、弹簧最大压缩/伸长、速度过零等。
- 动画循环时，不允许物体无故从画面消失或跳回；无限平面问题优先使用镜头跟随或滚动背景。

## 6. 参数设计

每题只开放“真正有教学价值”的参数。

参数分两类：

1. 独立参数：允许用户直接调节。
2. 派生参数：由题设约束和独立参数自动求解，不允许随意调节。

参数变化后必须同时更新：

- 物体几何位置
- 运动方程
- 力方向与大小
- 实时数值
- 曲线/图像
- 最终结论或趋势（若结论随参数改变）

## 7. 实时数据与图像

仅展示对理解题目有帮助的量：

- 位移、速度、加速度
- 力、冲量、动量、机械能
- 角度、张力、弹簧形变量
- 电势差、电场力、轨迹参数等

如题目本身含 v-t / x-t / F-t 图，动态图必须与实际运动同步，使用当前时刻游标。

物体自身速度优先直接标在物体附近；速度为 0 时显示 0，不画虚假箭头。

## 8. 透视与 3D / 2.5D

- 只有当透视能够帮助理解空间关系时才使用。
- 透视不得导致“高处看成低处”“向下看成向上”等物理误导。
- 相机变化不得改变物理量本身。
- 若 2D 更清晰，则优先 2D 教材风格，不为立体而立体。

## 9. 题目分析层

每道题制作前，AI 必须先写出内部规格：

1. 场景对象
2. 初始条件
3. 几何约束
4. 所有受力
5. 运动阶段/关键事件
6. 独立参数
7. 派生参数
8. 运动方程或守恒关系
9. 需要实时显示的量
10. 可能发生的错误

规格核查通过后才修改 HTML。

## 10. 验收标准

每题至少检查：

### 初始状态
- 与题图/题意一致
- 所有接触、连接正确
- 初始速度/力/位置正确

### 运动中间状态
- 受力方向正确
- 箭头起点不悬空
- 箭头长度随大小变化
- 标签无遮挡
- 物体不穿模
- 绳/弹簧不断开
- 参数变化后仍满足题设约束

### 关键事件
- 骤停、碰撞、反向、脱离等时刻正确
- 状态切换连续，无跳变

### 终止/循环状态
- 结论与物理方程一致
- 循环不制造不真实的位移跳跃

### 极值参数
- 每个可调参数取最小、中间、最大值分别测试
- 不得出现 NaN、无限大、出画面、方向翻转、穿模或错误结论

## 11. 自动化执行规则

交给 Codex / Hermes 时使用：

> 读取 PHYSICS_VISUAL_STANDARD_V1.md 与当前题目规格。先输出场景对象、约束、受力、运动方程、参数、关键事件和风险点；自检通过后再改代码。修改完成后按“初始 / 中间 / 关键事件 / 终止 / 极值参数”五类状态验收。不得为了视觉效果修改物理方向、接触关系或题设约束。最终只汇报：修改内容、物理核查结果、仍存在的不确定项。

## 12. 批量处理原则

批量处理前必须先用一批代表题验证模板：

- 弹簧/多物体
- 斜面/摩擦
- 绳/滑轮
- 碰撞
- 图像题
- 波动
- 电场

只有模板通过人工验收后，才允许批量改其余题。