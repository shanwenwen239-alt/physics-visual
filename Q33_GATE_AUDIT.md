# 第33题 Gate 核查记录

状态：**Gate 0–1 可核；Gate 2（原题几何）未通过；禁止生成新的最终 HTML。**

## Gate 0｜题意与对象
当前仓库页面能确认的对象：两相干波源 M、N；中垂线；a、b、c 三点；波传播与质点振动。

当前能确认的物理判断：若 M、N 同相且振动情况相同，则中垂线上任一点满足 r_M=r_N，故 Δr=0，为干涉加强位置。

但仓库中没有保存第33题的完整原题图片/原图几何约束，无法确认：
- a、b、c 在原图中的准确位置；
- a 是否由“波峰相遇”定义；
- c 是否由“离 a 最近的波谷相遇”定义；
- b 是否为某两点中点或其他指定点；
- 波源间距、波长与三点位置的精确关系；
- 原图是否包含波峰线/波谷线、实线/虚线等关键参照。

## Gate 1｜物理规格
若两波源同频、同相、等振幅，单源可写为：

u_M=A cos(ωt-kr_M),  u_N=A cos(ωt-kr_N)

合振动：

u=2A cos[k(r_M-r_N)/2] · cos[ωt-k(r_M+r_N)/2]

所以合振幅：

A_sum=2A |cos(Δφ/2)|,  Δφ=kΔr

加强条件：Δr=nλ；减弱条件：Δr=(n+1/2)λ。

中垂线上 Δr=0，属于加强位置。

## Gate 2｜原题几何规格
**未通过。**

当前 q33-expression-v2.html 把 a、b、c 直接固定在中垂线的三个任意像素 y=250/330/410；这些位置没有从原题条件和 λ 推导产生，因此不能视为原题几何复刻。

根据项目闸门规则：原题图承担空间/几何关系时，缺原图不得自行重构或猜测，所以本题在拿到原题图之前停止生成“完成版”。

## Gate 3｜世界模型 → 投影
待 Gate 2 补齐后执行。要求：
1. M、N、a、b、c 全部先在世界/平面坐标中定义；
2. 点的位置由题设几何和 λ 计算，不允许固定像素冒充物理位置；
3. 波峰/波谷线若为题意组成部分，必须由相位方程生成；
4. SVG 只负责投影和绘制。

## Gate 4｜自动自检（待实现）
至少检查：
- M、N 相位关系；
- a/b/c 的 Δr 是否满足题设；
- 若 a 定义为波峰相遇，当前初始相位必须真的满足两列波在 a 同为波峰；
- 若 c 定义为最近波谷相遇，必须证明与 a 的空间距离是满足条件的最小正值；
- 参数变化后，三点若属于“原题固定点”，不得随意失去原题定义；若探索模式允许重算，则必须由方程重新生成。

## 结论
当前第33题旧页面只能作为“中垂线上 Δr=0 的概念演示”，不能作为原题最终版。需要原题完整图片后才能通过 Gate 2 并重做。