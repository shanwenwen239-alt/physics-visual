# 第33题 Gate 核查记录

状态：**已取得完整原题图，Gate 0–4 通过，可生成原题版 HTML。**

## Gate 0｜题意与对象
原题明确：水平面上有 M、N 两个振动情况完全相同的振源；MN 连线的中垂线上有 a、b、c 三点。某时刻 a 点是两列波的波峰相遇点；c 点是与 a 点相距最近的两波谷相遇点；b 点处在 a、c 正中间。

原图固定几何：M、N 水平放置；MN 为水平虚线；其中垂线为竖直虚线；a、b、c 位于中垂线上且依次向下；b 是 a、c 的几何中点。

## Gate 1｜物理规格
两振源同频、同相、等振幅：

u_M=A cos(ωt-kr_M+φ0),  u_N=A cos(ωt-kr_N+φ0)

中垂线上任一点 r_M=r_N，因此 Δr=0，合振幅恒为 2A，a、b、c 都是干涉加强点。所谓“波峰/波谷相遇”描述的是某一时刻的瞬时相位，不改变该点的干涉加强属性。

取该题所示时刻为 t=0，并选择 φ0 使 a 点两波均为波峰。沿中垂线向下寻找与 a 最近的两波谷相遇点 c，必须满足单源传播距离相对 a 增加 λ/2：

r_c = r_a + λ/2。

若 M=(-d/2,0)、N=(d/2,0)、a=(0,y_a)，则
r_a=sqrt((d/2)^2+y_a^2)，
r_c=r_a+λ/2，
y_c=sqrt(r_c^2-(d/2)^2)。

b=(0,(y_a+y_c)/2)。注意：b 是空间位置中点，但由于 r(y)=sqrt((d/2)^2+y^2) 非线性，b 在该瞬间不必恰好处于平衡位置；不能人为把 b 画成 y=0。

半个周期后所有点的瞬时位移反号，但 Δr 仍为 0，所以 a、b、c 仍全部是加强点。原题正确选项：D。

## Gate 2｜原题几何规格
通过。HTML 必须复刻：
1. M、N 水平对称；
2. MN 水平虚线；
3. 竖直中垂线虚线；
4. a、b、c 位于中垂线并按原图上下顺序排列；
5. b 严格为 a、c 的几何中点；
6. 镜头、参照系、比例固定，不做相机缩放；
7. 波纹只能作为动态叠加层，不能破坏原题母版。

## Gate 3｜世界模型 → 屏幕投影
M、N、a、b、c 先在物理平面坐标中定义。a 给定后，c 由 r_c=r_a+λ/2 求解，b 再由几何中点求得。屏幕坐标仅使用统一比例和平移映射，不允许固定像素位置冒充物理点。

波峰/波谷环由相位条件生成。初始时刻相位偏置由 a 的波峰条件确定，因此 c 自动成为最近的波谷相遇点。

## Gate 4｜自动自检
页面必须实时检查：
- |r_M-r_N| 在 a/b/c 均≈0；
- b_y=(a_y+c_y)/2；
- r_c-r_a=λ/2；
- t=0 时 a 两列波相位为波峰；
- t=0 时 c 两列波相位为波谷；
- 三点合振幅均为 2A；
- 半周期后三点仍为加强点。

任一检查失败不得标记为最终版。

## 展示策略
本题不需要让 a、b、c 位置随动画乱动。固定原题几何母版；播放时展示波峰/波谷传播及三点瞬时振动。参数探索时 λ 或源间距改变，c 和 b 必须按题设重新计算，确保仍然满足“c 是离 a 最近的波谷相遇点、b 是中点”。