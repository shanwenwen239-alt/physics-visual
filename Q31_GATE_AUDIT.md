# 第31题 Gate 核查记录

## Gate 0：题意与展示目标

当前题目用于说明走钢丝时使用长杆为什么能提高抗扰动能力。该题的核心不是求某个真实人体角度或角速度，而是比较：在相同外界扰动力矩下，杆越长，系统转动惯量越大，角响应越慢。

因此本题不适合伪装成完整人体动力学连续仿真，采用“固定参照 + 参数交互 + 相对比较”形式。

结论：通过。

## Gate 1：物理规格

### 研究对象
人 + 横杆，绕钢丝附近的等效转动轴发生小角度响应。

### 物理关系
横杆若近似为均匀细杆，关于中点轴的转动惯量：

I_rod = (1/12) m_r L^2

整体等效转动惯量：

I_total = I_body + I_rod

在同一外界扰动力矩 tau 下：

alpha = tau / I_total

因此在 I_body、m_r、tau 相同的比较条件下：

L ↑ -> I_total ↑ -> |alpha| ↓

### 禁止项
- 不把模型输出称为真实人体精确角度；
- 不用人为比例制造“真实角速度”；
- 不模拟未经题设支持的周期摆动；
- 不把人体主动平衡反馈混入本简化模型。

结论：通过。

## Gate 2：原图/参照规格

本题的核心比较基准是钢丝与人体的固定参照，而不是复杂原图空间几何。因此：

- 钢丝位置锁定；
- 人体中心位置锁定；
- 杆保持水平穿过人体；
- 改变 L 时只改变杆长度，不缩放镜头、不移动钢丝；
- 不通过相机变化制造“更稳定”的错觉。

结论：通过。

## Gate 3：参数分类

### 独立物理参数
- 杆长 L
- 杆质量 m_r（探索模式）
- 相对扰动力矩 tau_rel（只作为比较强度，不标成未经标定的 N·m）

### 固定比较参数
- 人体等效转动惯量 I_body_rel 取统一基准值；只用于相对比较，不宣称真实人体测量值。

### 派生量
- I_total_rel
- alpha_rel = tau_rel / I_total_rel

结论：通过。

## Gate 4：代码/物理一致性要求

1. 杆长度只改变几何杆长和 I_rod；
2. 相对转动惯量必须按 L^2 变化；
3. alpha_rel 必须严格由 tau_rel / I_total_rel 计算；
4. 页面不输出伪精确真实角度、角速度；
5. 固定镜头，短杆/长杆可以直接视觉比较。

结论：通过，可以生成最终 HTML。

## Gate 5：最终验收项目

- L 最小/最大时杆不出界；
- 切换 L 时钢丝、人中心不移动；
- I_total 随 L 单调增大；
- alpha_rel 随 L 单调减小；
- 重置恢复默认；
- 不存在自动连续倾倒动画；
- 页面明确标注“相对比较模型，不是人体精确动力学”。