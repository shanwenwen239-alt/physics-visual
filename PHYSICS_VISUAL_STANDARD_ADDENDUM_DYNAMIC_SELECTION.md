# 物理可视化统一标准补充：动态表达适配规则

> 本补充规则与 `PHYSICS_VISUAL_STANDARD_V1.md` 同级执行。生成、修改或验收题目时，必须先判断“该物理量是否适合通过主体连续运动直接表达”，不得默认所有物理量都做成动画。

## 1. 核心原则（必须执行）

**不是所有物理量都必须通过主体运动直接可视化。**

- 对位置、速度、轨迹、碰撞、形变量等适合动态展示的量，优先使用连续动画。
- 对加速度、功、能量、动量、力矩、守恒量、极值条件等难以直接从运动外观辨认的量，优先使用矢量、图像、状态对照、能量流、系统量视图或关键时刻卡片表达。
- 若连续动画不能显著提升理解，则不应为了“动态”而动态。

## 2. 强制判断准则

**如果学生关闭数值后，仍然无法仅凭动画可靠看出某个物理量的变化趋势，就不要把它当作主要动态表达对象。**

此时应将主体动画降为“提供物理过程和空间背景”，把真正需要理解的量交给更合适的表达层。

## 3. 默认表达方式

| 物理量/关系 | 优先表达方式 |
|---|---|
| 位置、位移、轨迹 | 主体连续动画 |
| 速度 | 主体运动 + 速度矢量；必要时 v-t 图 |
| 加速度 | 加速度矢量、a-t 图、合力对应、关键状态对照 |
| 力 | 真实作用点受力箭头；必要时独立受力图 |
| 功 | 能量流、累计功、系统边界，不强行用主体运动表现 |
| 动能、势能、弹性势能 | 能量仓、能量流、状态对照、系统量视图 |
| 动量、冲量 | 动量矢量、碰撞前后状态、系统总动量视图 |
| 力矩 | 力臂 + 力矢量 + 转动趋势/关键状态 |
| 守恒关系 | 系统边界 + 总量视图 + 前后/过程对照 |
| 极值条件 | 关键时刻冻结、高亮条件、局部图像/导数趋势 |
| 几何比较 | 固定参照系、固定比例、状态叠加或拖动比较 |

## 4. 每题制作前新增的强制分析项

在原有“题目分析层”基础上，增加：

14. 逐项列出本题核心物理量，并判断：主动画可直接感知 / 需要辅助表达 / 不适合连续动画。
15. 指定每个核心物理量的首选视觉编码方式，禁止所有量机械套用同一种图表或动画模板。
16. 判断关闭实时数值后，学生是否仍能从视觉上正确判断该量的方向、趋势或关键状态；若不能，必须增加适合的辅助表达。
17. 明确主动画承担什么教学任务，辅助层承担什么教学任务，避免重复展示和视觉堆积。

## 5. 验收新增项

每题完成后额外检查：

- 主动画是否真的帮助理解，而不是装饰性运动。
- 是否存在“动画在动，但学生无法看出题目真正要比较的物理量”的情况。
- 加速度、功、能量、动量、力矩、守恒量、极值条件是否采用了更直接的视觉编码。
- 关闭数值后，核心趋势是否仍可理解；若不可理解，辅助表达是否足以补足。
- 辅助表达是否与同一套物理方程实时同步，而不是独立演示。
- 若连续动画价值很低，应允许改成参数拖动、关键状态切换、前后对照或静态+交互，而不是强制播放。

## 6. 对 Codex / Hermes / Claude / WorkBuddy 的执行指令

> 在修改任何题目之前，先判断每个核心物理量是否适合主体连续动画。不要默认“物理可视化=所有量都动画化”。位置、速度、轨迹、碰撞、形变量优先连续动画；加速度、功、能量、动量、力矩、守恒量、极值条件优先选择矢量、图像、状态对照、能量流、系统量视图或关键时刻卡片。如果关闭数值后仍无法仅凭主体动画可靠看出某物理量的变化趋势，则该量不得作为主体动画的主要表达对象，必须改用更直接的视觉编码。若连续动画不能显著提升理解，允许并优先使用参数拖动、状态切换、关键事件冻结或前后对照。所有辅助表达必须与同一物理模型实时同步。
